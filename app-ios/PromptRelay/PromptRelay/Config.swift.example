// =============================================================================
// Config.swift — アプリ設定テンプレート
// =============================================================================
//
// 使い方:
//   1. このファイルを同じディレクトリに Config.swift としてコピー
//      cp Config.swift.example Config.swift
//   2. 必要に応じて defaultServerURL を書き換え（空文字列のままでもOK）
//   3. Config.swift は .gitignore 済みなのでコミットされません
//
// =============================================================================

enum AppConfig {
    // prompt-relay サーバの URL（デフォルト値）
    // 空文字列の場合、アプリの設定画面で入力が必要です
    // 固定値を設定すると初回起動時のデフォルトとして使われます
    // 例: "http://192.168.x.x:3939", "https://your-host.tail-xxxxx.ts.net:3940"
    static let defaultServerURL = ""
}
